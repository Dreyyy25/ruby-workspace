# Hirb is older than this IRB and crashes when it prints an assignment (x = ...),
# so do not print assignments. Type the variable name on its own line to see it.
IRB.conf[:ECHO_ON_ASSIGNMENT] = false
